export const RESET_CSS = `
.reactjs-player-media {
  button {
    background: transparent;
    outline: none;
    border: none;
    padding: 0;
    margin: 0;
    cursor: pointer;
    border-radius: 4px;
  }
}
`;
